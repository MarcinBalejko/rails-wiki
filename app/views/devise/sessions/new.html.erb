
<br>
<div class="special col-md-5 card mt-3 " >
  <div class="card-container">
      <br>
      <h3 class="card-header d-flex justify-content-center" > Log in </h3>
      <div class="card-body">

      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="field">
          <p class="login-labels">Email<p>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'login-forms' %>
        </div>

        <div class="field">
          <p class="login-labels">Password</p>
          <%= f.password_field :password, autocomplete: "current-password", class: 'login-forms' %>
        </div>

        <% if devise_mapping.rememberable? %>
          <div class="field" style="text-align: center;">
            <%= f.check_box :remember_me %>
            <%= f.label :remember_me, style: 'margin-bottom: 10px;' %>
          </div>
        <% end %>

        <div class="actions" style="text-align: center;">
          <%= f.submit "Log in", class: 'btn', style: 'margin-bottom: 5px;' %>
        </div>
      <% end %>
      <br>
      
      <%= render "devise/shared/links" %>

    </div>
  </div>
</div>
      
