
<br>
<div class="special col-md-5 card mt-3 " >
  <div class="card-container">
    <br>
    <h4 class="card-header d-flex justify-content-center" > Forgot your password? </h3>
      <div class="card-body">

        <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>

          <div class="field">
            <p class="login-labels">Email<p>
            <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'login-forms' %>
          </div>

          <div class="actions" style="text-align: center;">
            <%= f.submit "Send password reset instructions"%>
          </div>
        <% end %>

        <br>
        <p style="text-align: center;"><%= render "devise/shared/links" %></p>

      </div>
  </div>
</div>





















      
