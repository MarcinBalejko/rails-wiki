
<br>
<div class="special col-md-5 card mt-3 " >
  <div class="card-container">
      <br>
      <h3 class="card-header d-flex justify-content-center" > Sign up </h3>
      <div class="card-body">

      <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <p class="alert"><%= render "devise/shared/error_messages", resource: resource %></p>

        <div class="field">
          <p class="login-labels">Email<p>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'login-forms', style: 'margin: 0;' %>
        </div>

        <div class="field">
          <p class="login-labels">Password</p>
          <% if @minimum_password_length %>
          <% end %>
          <%= f.password_field :password, autocomplete: "new-password", class: 'login-forms' %>
        </div>
        
        <div class="field">
          <p class="login-labels">Password Confirmation<p>
          <%= f.password_field :password_confirmation, autocomplete: "new-password", class: 'login-forms'%>
        </div>

        <div class="actions" style="text-align: center;">
          <%= f.submit "Sign up", class: 'btn', style: 'margin-bottom: 5px;' %>
        </div>
      <% end %>

      <br>

      <p style="text-align: center;"><%= render "devise/shared/links" %></p>

    </div>
  </div>
</div>

